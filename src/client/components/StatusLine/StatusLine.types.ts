export type StepType =
  | 'first-step'
  | 'last-step'
  | 'intermediate-step'
  | 'single-step';

export type {
  AltDocumentContent,
  StatusLine,
  StatusLineItem,
} from '../../../universal/types';
