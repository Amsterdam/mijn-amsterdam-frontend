import { WMOVoorzieningFrontend } from './wmo';

describe('Transform api items', () => {
  const compareDate = new Date(2022, 2, 11);

  test('It workx', () => {
    expect(true).toBe(true);
  });
});
