openapi: 3.0.3
info:
  version: '1.0'
  title: Mijn Amsterdam api spec for Mijn Amsterdam - Dienstverlening api's
  description: |
    We're providing a set of endpoints that can be used to retrieve data from source api's. These api's are intended for users other than Mijn Amsterdam
    Note: the paths that start with /private are only accessible in development mode (localhost) or via our private-network resources.
  contact:
    name: Mijn Amsterdam
    url: https://mijn.amsterdam.nl/
    email: mijnAmsterdam@amsterdam.nl
paths:
  /api/v1/status/health:
    get:
      description: Endpoint for checking whether the server has started.
      responses:
        '200':
          description: Returns "OK" when server is started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthyResponse'

  /private/api/v1/services/wmo/voorziening:
    post:
      security:
        - bearerAuth: []
        - apimSubscriptionKey: []
      description: |
        Gets a single WMO voorziening by id.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bsn, id]
              properties:
                bsn:
                  type: string
                  description: The BSN of the user to fetch the WMO voorziening for.
                id:
                  type: string
                  description: Id of the voorziening.
      responses:
        '200':
          description: |
            The requested WMO voorziening
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/WmoVoorzieningenResponse'
        '500':
          $ref: '#/components/responses/ApplicationErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailableResponse'
        '400':
          $ref: '#/components/responses/BadRequestResponse'
        '404':
          $ref: '#/components/responses/NotFoundResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedResponse'

  /private/api/v1/services/wmo/voorzieningen:
    post:
      security:
        - bearerAuth: []
        - apimSubscriptionKey: []
      description: |
        Gets a list of WMO voorzieningen.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bsn]
              properties:
                bsn:
                  type: string
                  description: The BSN of the user to fetch the WMO voorzieningen for.
                maActies:
                  type: array
                  items:
                    $ref: '#/components/schemas/WmoVoorziening/properties/maActies'
                  description:
                    Optional list of maActies to filter the voorzieningen on.
                    Only voorzieningen that have at least one of the specified maActies will be included in the response.
                maProductgroep:
                  type: array
                  items:
                    $ref: '#/components/schemas/WmoVoorziening/properties/maProductgroep'
                  description:
                    Optional list of maProductgroepen to filter the voorzieningen on.
                    Only voorzieningen that have at least one of the specified maProductgroepen will be included in the response.
      responses:
        '200':
          description: |
            A list of huidige toegewezen WMO voorzieningen sorted by datumBesluit desc.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/WmoVoorzieningenResponse'
        '500':
          $ref: '#/components/responses/ApplicationErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailableResponse'
        '400':
          $ref: '#/components/responses/BadRequestResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedResponse'

  /private/api/v1/services/afis/e-mandates/sign-request-status-notify:
    post:
      security:
        - bearerAuth: []
        - apimSubscriptionKey: []
      description: |
        Endpoint to receive push notifications from AFIS about the status of e-mandate sign requests.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AfisStatusNotificationRequestBody'
      responses:
        '200':
          description: |
            Acknowledgement of the received notification.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/AfisStatusNotificationResponse'
        '500':
          description: |
            Returns Application error, any error in code or from failed requests.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/AfisApplicationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailableResponse'
        '400':
          $ref: '#/components/responses/BadRequestResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedResponse'

components:
  securitySchemes:
    apimSubscriptionKey:
      description: Apim subscription key provided via https://devportal.api.amsterdam.nl/
      type: apiKey
      name: Ocp-Apim-Subscription-Key
      in: header
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    BadRequestResponse:
      description: Returns bad request error
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/ErrorBadRequest'
    NotFoundResponse:
      description: Returns not found error
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/ErrorNotFound'
    UnauthorizedResponse:
      description: Returns unauthorized error (invalid api key / token)
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/ErrorUnauthorized'
    ServiceUnavailableResponse:
      description: Returns Service Unavailable error
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/ErrorUnavailable'
    ApplicationErrorResponse:
      description: |
        Returns Application error, any error in code or from failed requests.
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/ApplicationError'
  schemas:
    HealthyResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - OK
        content:
          type: string
          enum:
            - OK
    WmoVoorzieningenResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/WmoVoorziening'
        status:
          type: string
          enum:
            - 'SUCCESS'
    WmoVoorziening:
      type: object
      properties:
        id:
          type: string
        titel:
          type: string
        maProductgroep:
          type: string
          enum:
            - 'WRA'
            - 'hulpmiddelen'
            - 'diensten'
            - 'PGB'
            - 'vergoeding'
            - 'AOV'
            - 'Alle afgewezen'
        maActies:
          type: array
          items:
            type: string
            enum:
              - stopzetten
              - reparatieverzoek
        beschikkingNummer:
          type: integer
          nullable: true
        productIdentificatie:
          type: string
          nullable: true
        procesIdentificatie:
          type: string
          nullable: true
        procesMeldingIdentificatie:
          type: string
          nullable: true
        beschiktProductIdentificatie:
          type: string
          nullable: true
        datumBesluit:
          type: string
          nullable: true
        datumAanvraag:
          type: string
        datumBeginLevering:
          type: string
          nullable: true
        datumEindeGeldigheid:
          type: string
          nullable: true
        datumEindeLevering:
          type: string
          nullable: true
        datumIngangGeldigheid:
          type: string
          nullable: true
        datumOpdrachtLevering:
          type: string
          nullable: true
        leverancier:
          type: string
          nullable: true
        leverancierIdentificatie:
          type: string
          nullable: true
        leveringsVorm:
          type: string
          nullable: true
        productsoortCode:
          type: string
          nullable: true
        resultaat:
          type: string
          enum:
            - toegewezen
            - afgewezen
    AfisStatusNotificationRequestBody:
      type: object
      properties:
        id_client:
          type: string
          example: '1000'
        debtornumber:
          type: string
          example: '123456'
        cid:
          type: string
          example: '2345678910'
        mpid:
          type: string
          example: '1234567890'
        payment_reference:
          type: string
          example: '123456789'
        id_request_client:
          type: string
          example: 'test'
        event_type:
          type: string
          example: 'payment'
        amount_total:
          type: string
          example: '45500'
        id_bank:
          type: string
          example: 'ABNANL2A'
        iban:
          type: string
          example: 'GB33BUKB20201555555555'
        bic:
          type: string
          example: 'INGBNL2A'
        account_owner:
          type: string
          example: 'John Doe'
        event_date:
          type: string
          format: date
          example: '2024-01-05'
        event_time:
          type: string
          format: time
          example: '11:27'
        variable1:
          type: string
          example: 'GB33BUKB20201555555555'
    AfisStatusNotificationResponse:
      type: object
      properties:
        content:
          type: string
          enum:
            - 'E-Mandate created successfully'
        status:
          type: string
          enum:
            - 'SUCCESS'
    ApplicationError:
      type: object
      description: |
        Error if a part of the application returns an error, mostly dependency errors to remote systems.
      properties:
        content:
          nullable: true
        code:
          type: number
          enum:
            - 500
        status:
          type: string
          enum:
            - 'ERROR'
        message:
          type: string
    AfisApplicationError:
      allOf:
        - $ref: '#/components/schemas/ApplicationError'
        - type: object
          properties:
            message:
              type: string
              enum:
                - 'Failed to create E-Mandate from sign request status notification'
    ErrorUnavailable:
      type: object
      properties:
        content:
          nullable: true
        code:
          type: number
          enum:
            - 503
        status:
          type: string
          enum:
            - 'ERROR'
        message:
          type: string
          enum:
            - 'Service Unavailable'
    ErrorNotFound:
      type: object
      properties:
        content:
          nullable: true
        code:
          type: number
          enum:
            - 404
        status:
          type: string
          enum:
            - 'ERROR'
        message:
          type: string
          enum:
            - 'Not Found'
    ErrorUnauthorized:
      type: object
      properties:
        content:
          nullable: true
        code:
          type: number
          enum:
            - 401
        status:
          type: string
          enum:
            - 'ERROR'
        message:
          type: string
          enum:
            - 'Api key ongeldig'
            - 'Authorisatie header mist'
            - 'Authorisatie header verkeerd gevormd'
            - 'Unauthorized'
    ErrorBadRequest:
      type: object
      properties:
        content:
          nullable: true
        code:
          type: number
          enum:
            - 400
        status:
          type: string
          enum:
            - 'ERROR'
        message:
          type: string
          example: 'Invalid input'
