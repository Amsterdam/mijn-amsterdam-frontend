trigger:
  - ontwikkelen

pr:
  branches:
    include:
      - ontwikkelen

variables:
  - ${{ if eq($[Build.SourceBranchName], 'ontwikkelen') }}:
      - group: MAPipelineVariables-o

steps:
  - bash: |
      echo $(serviceConnectionName)
  - bash: |
      printenv
