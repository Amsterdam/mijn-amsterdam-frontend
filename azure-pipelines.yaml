trigger: none
pr: none

resources:
  repositories:
    - repository: MamsInfra
      type: git
      name: MijnAmsterdam/mijn-amsterdam-infra
      ref: refs/heads/main

parameters:
  - name: btdBuild
    type: boolean
    default: true
  - name: btdTest
    type: boolean
    default: true
  - name: btdDeploy
    type: boolean
    default: true
  - name: bffImageTag
    displayName: BFF image tag used if btdBuild is false. Examples bff-$(Build.BuildId),bff-latest
    type: string
    default: bff-$(Build.BuildId)
  - name: dtapName
    type: string
    default: t
    values:
      - t
      - a
  - name: btdAppBFF
    type: boolean
    default: true
  - name: btdAppUI
    type: boolean
    default: true

variables:
  - template: pipelines/pool.yaml@MamsInfra
    parameters:
      dtapName: ${{ parameters.dtapName }}

jobs:
  - template: pipelines/jobs/apps/btd-frontend.yaml@MamsInfra
    parameters:
      pool: ${{ variables.pool }}
      dtapName: ${{ parameters.dtapName }}
      btdBuild: ${{ parameters.btdBuild }}
      btdTest: ${{ parameters.btdTest }}
      btdDeploy: ${{ parameters.btdDeploy }}
      bffImageTag: ${{ parameters.bffImageTag }}
      btdAppBFF: ${{ parameters.btdAppBFF }}
      btdAppUI: ${{ parameters.btdAppUI }}
