name: 'Release notification'
# on: create
on:
  pull_request:
    types: [opened, edited, synchronize]
jobs:
  create-notification:
    # if: ${{ contains(github.ref, 'MIJN-6651') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Create release notification
        run: npm run release:notification
        env:
          webhook-host: ${{ secrets.MS_TEAMS_WEBHOOK_HOST }}
          webhook-path: ${{ secrets.MS_TEAMS_RELEASE_NOTIFICATION_WEBHOOK_PATH }}
