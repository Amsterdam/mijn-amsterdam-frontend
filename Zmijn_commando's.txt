pnpm run serve-dev


cp -i .env.local.template .env.local

TESTEN:
pnpm run bff-api:test
pnpm test

NAAR TEST
./scripts/deploy-to-env

Main mergen in huidige branch:
git checkout Main
git pull
git checkout -   > is terug naar vorige branche
git merge main
